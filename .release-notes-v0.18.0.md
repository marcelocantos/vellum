## Added

- Optional static in-document table of contents: `style.toc` (config, MCP, or Go `convert.Request`) or a `<!-- vellum:toc -->` hint injects nested heading links into convert HTML. No JavaScript. PDF adds dotted leaders and page numbers. The viewer article shows the same block.

## Changed

- Third-party attribution file renamed `THIRD_PARTY_NOTICES.md` → `NOTICE`.
- GitHub repo description shortened so Homebrew formula generation does not truncate mid-word.
