## Changed

- Releases ship locally via `cv release` (gate → dist → GitHub release → Homebrew tap through tapper). GitHub Actions CI and release workflows removed.
- Viewer TOC sidebar is resizable (drag the splitter; width persists in localStorage).
