## Fixed

- Viewer task-list toggles adopt the source stamp without a full page reload, so Space after Tab keeps keyboard focus on the same checkbox.
- When a watch event does reload the view (for example an external edit), scroll position and the focused checkbox are restored.

## Changed

- Converted HTML without math no longer injects the KaTeX jsDelivr stylesheet.
